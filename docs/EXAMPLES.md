# Examples Index

- `examples/example1/` — legacy full CRUD + relationships sample
- `examples/minimal/` — modern minimal example for quick onboarding

## Run minimal example

```bash
php examples/minimal/run.php
```

This example uses an in-memory SQLite database so it can run quickly without external setup.
