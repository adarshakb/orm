# Minimal Example

This example shows the smallest end-to-end usage:

1. Inject in-memory SQLite connection
2. Create a `users` table
3. Define a `User` model extending `DataBoundObject`
4. Insert and load a row

Run:

```bash
php examples/minimal/run.php
```
